import { supabase as db } from '../../omc-backend-core';

export default (token, callback) => {
    /*db.from('user_chats')
        .select('chat_id')
        .eq('user_id', token)
        .then(({ data, error }) => {
            callback(data);
        });*/
};